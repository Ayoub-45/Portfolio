import React from "react";

export default function Logo() {
  return (
    <img
      src="../../../Ayoub.ico"
      style={{
        cursor: "pointer",
        width: "120px",
        height: "120px",
        color: "black",
        borderRadius: "50%",
        marginTop: "-25px",
      }}
      alt="logo"
    />
  );
}
